// Teams layout — AppShell is already provided by the parent app/app/layout.tsx
// This layout exists for any teams-specific providers or wrappers if needed

export default function TeamsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
